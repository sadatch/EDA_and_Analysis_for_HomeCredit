pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2
torch>=2.1
scipy>=1.11
pyarrow>=14.0
optuna>=3.5
# GPU補足:
#  - XGBoost: pip版で device="cuda" がそのまま使える（追加ビルド不要）
#  - CatBoost: pip版で task_type="GPU" がそのまま使える（追加ビルド不要）
#  - LightGBM: GPU(OpenCL)版は別途ビルドが必要。未対応環境では自動でCPUへフォールバックする
#      例: pip install lightgbm --config-settings=cmake.define.USE_GPU=ON
#  - torch: CUDA対応版を https://pytorch.org の指示に従って入れること（DAE/MLPのGPU学習用）
